### Первый агентский опыт

1. В локальный CLAUDE.md я прописал только то, что было указано в качестве примера. Спросив агента что он знает о проекте, сразу бросилось в глаза "без сервис объектов".
Для меня это момент правки, т.к. проект будет использовать ServiceObject для импорта, экспорта, внешнего api. 

2. Раньше мне не приходилось работать с агентами осознанно и я предполагал, что агент имеет доступ только к папке проекта(путь откуда запущен агент). Выяснилось, что 
он читает /Users/dniman/.claude/CLAUDE.md, а значит может стучаться к файлам и папкам за пределами проекта, но по умолчанию работает в рамках текущего каталога.
А что если случайно изменить папку текущего проекта, в этом случае он сможет явно обращаться к файлам, которые ему не должны быть доступны ?
А что если разработчики изменят режим "по умолчанию", а я об этом даже знать не буду и это уже не безопасно, а может я и загоняюсь)))
Работают ли остальные агенты(Codex, Cursor agents) таким же образом?

Приходит на ум, что нужно в файле CLAUDE.md явно задать рабочий каталог и структуру проекта.

3. Попросил агента создать папку homeworks/hw-0. Удивило то, что предложил создать (Да, Нет) и отдельно был пункт на получение доступа
4. Попросил агента создать файлы promt.md и report.md. Уже без вопросов их создал.
5. Попросил агента открыть файл /Users/dniman/Projects/ai/to/homeworks/hw-0/report.md в Obsidian. Он написал, файл открыт в Obsidian( ответ получил "файл открыт в Obsidian"), но на деле ничего не произошло. Obsidian работает только со своим хранилищем, команда не возвратила ошибку
Решил попробовать открыть файл в mate и все отработало как надо.

Запуск prompt.md

Вот что было исправлено:
  config/routes.rb
  - get "home/index" → get "/home", to: "home#index", as: :home
  - Теперь маршрут /home существует и имеет хелпер home_path

  app/controllers/application_controller.rb
  - Добавлен after_sign_in_path_for — Devise теперь редиректит на /home после логина

  Что уже было корректно:
  - HomeController — before_action :authenticate_user! стоит, защита работает
  - root "home#index" — при попытке зайти на / неавторизованный пользователь тоже получит редирект на логин
  - Devise views для sessions есть, регистрация отключена через skip: [:registrations]

  Сценарий работы:
  1. Пользователь заходит на /home → редирект на /users/sign_in
  2. Вводит test@example.com / password123 → редирект на /home

